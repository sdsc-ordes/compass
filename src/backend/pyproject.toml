[project]
name = "backend"
version = "0.1.0"
description = "Compass API: SHACL-driven filters and SPARQL over the ontology"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.135.3",
    "geojson>=3.2.0",
    "pyoxigraph>=0.3.0",
    "pyshacl>=0.31.0",
    "rdflib>=7.6.0",
    "httpx>=0.27",
    "uvicorn>=0.42.0",
]

[dependency-groups]
dev = [
    "hypothesis>=6.100",
    "pytest>=8.0",
    "ruff>=0.9,<1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
