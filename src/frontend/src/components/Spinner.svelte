<script lang="ts">
  /**
   * The widget's one loading indicator.
   *
   * Used by both waits: the story count in the tally box and the map plate while
   * the engine boots. A ring rather than anything of our own — a wait is the one
   * place a visitor is better served by the convention they already know than by
   * something with personality.
   *
   * Decoration, and aria-hidden with it. What says a wait is running is the
   * tally's live region and the plate's own text.
   */
</script>

<div class="spinner" aria-hidden="true"></div>
